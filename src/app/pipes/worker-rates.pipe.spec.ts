import { WorkerRatesPipe } from './worker-rates.pipe';

describe('WorkerRatesPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkerRatesPipe();
    expect(pipe).toBeTruthy();
  });
});
