import { EscKeyDirective } from './esc-key.directive';

describe('EscKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new EscKeyDirective();
    expect(directive).toBeTruthy();
  });
});
